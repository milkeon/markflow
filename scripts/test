#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

# 워크스페이스 유닛 테스트 — test 스크립트가 있는 패키지만 실행(vitest).
# 현재: packages/shared(계약 zod). 다른 패키지가 test 스크립트를 추가하면 자동 포함.
echo "▸ Tests (pnpm -r test)…"
pnpm -r test
